% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/universe.R
\name{generate_universe_sysdata}
\alias{generate_universe_sysdata}
\title{Generate sysdata.rda for a package universe}
\usage{
generate_universe_sysdata(
  packages,
  prefix,
  extra_vars = list(),
  include_scanner_defaults = FALSE,
  file = "R/sysdata.rda"
)
}
\arguments{
\item{packages}{Character vector of package names.}

\item{prefix}{Character scalar used to name the saved objects
(e.g., \code{"stan"} produces \code{.stan_pkgs}).}

\item{extra_vars}{Named list of additional objects to include in
the saved file (e.g., citation environments).}

\item{include_scanner_defaults}{If \code{TRUE}, also saves \code{.stdlib_funs}
and \code{.scan_skip_dirs}. Defaults to \code{FALSE}.}

\item{file}{Output path. Defaults to \code{"R/sysdata.rda"}.}
}
\value{
Invisibly returns the result of \code{\link[=build_universe_data]{build_universe_data()}}.
}
\description{
Computes scanner data for the given packages and saves it to
\code{sysdata.rda} with variable names prefixed by \code{prefix}. This is
intended for use in a downstream package's \code{data-raw/sysdata.R}
script.
}
\details{
The generated variables are:
\describe{
\item{\verb{.\{prefix\}_pkgs}}{Character vector of package names.}
\item{\verb{.\{prefix\}_exports}}{Named list of exported functions per package.}
\item{\verb{.\{prefix\}_export_index}}{Inverted index: function name to packages.}
\item{\verb{.\{prefix\}_origin_map}}{Environment: \code{"pkg::fun"} keys to origin.}
\item{\verb{.\{prefix\}_pkg_versions}}{Named list of version strings.}
}

When \code{include_scanner_defaults} is \code{TRUE}, \code{.stdlib_funs} and
\code{.scan_skip_dirs} are also saved.
}
\examples{
file <- tempfile(fileext = ".rda")
generate_universe_sysdata(c("stats", "utils"), "my", file = file)
unlink(file)
}
